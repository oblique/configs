.config/gtk-3.0/bookmarks
.config/mpv/cache
.config/nvim/plugin/packer_compiled.lua
.mpv/cache

# Install the following only on my laptops
{{ if not (eq .chezmoi.hostname "dystopia" "netsphere") }}
.init_linux_console_colors.py
{{ end }}

# Install the following only on my laptops but not if user is root
{{ if or
    (not (eq .chezmoi.hostname "dystopia" "netsphere"))
    (eq .chezmoi.username "root")
}}
.config/alacritty
.fonts
.mpv
{{ end }}
